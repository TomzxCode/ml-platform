# Reference

The precise definitions: every CLI command and every YAML spec.

| Section | Contents |
|---|---|
| [CLI](cli/index.md) | One page per command group, with arguments and behavior |
| [Specs](specs/index.md) | The YAML files accepted by `job submit`, `dataset create`, `experiment create`, and `infra cluster create` |

For the conventions shared by all commands (global options, JSON mode, exit codes), see [CLI conventions](../guides/cli-conventions.md).
